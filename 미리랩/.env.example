# 이 파일을 복사해서 .env 로 저장한 뒤, 본인 키를 채우세요.
# .env 는 .gitignore 에 있어 절대 커밋되지 않습니다. 코드에 키를 직접 쓰지 마세요.

# OpenAI API 키 (필수: 실제 모드로 돌릴 때)
OPENAI_API_KEY=sk-your-key-here

# 사용할 모델 (기본 gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# (선택) KoBERT 감성분석 사용 시 1 로 설정. 기본은 LLM 점수 기반 휴리스틱.
USE_KOBERT=0
